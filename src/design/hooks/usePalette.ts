import { THEMES } from "../colors/light";
import { XLogsPalette } from "../colors/types";

function usePalette(): XLogsPalette {
  const palette = THEMES.light;
  return palette;
}

export default usePalette;
